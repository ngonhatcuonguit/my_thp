<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/toast_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/toast_warning_shape"
        android:gravity="center"
        android:padding="12dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/toast_icon"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/ic_tick_white_background_circle" />

        <com.cuongngo.my_thp.common.view.view_ex.TextViewEx
            style="@style/TextViewEx.BodyLarge"
            android:id="@+id/toast_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/_6dp"
            android:textColor="@color/black_1c"
            tools:text="This is Custom Toast." />
    </androidx.appcompat.widget.LinearLayoutCompat>
</RelativeLayout>
